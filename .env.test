# Test Database (Docker Compose or CI service container)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/ai_sessions_test"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="test-secret-key-for-e2e-tests-only"

# GitHub OAuth (use test OAuth app or mock values for local testing)
GITHUB_CLIENT_ID="test-github-client-id"
GITHUB_CLIENT_SECRET="test-github-client-secret"

# Optional services - disabled for tests
# GOOGLE_CLOUD_PROJECT=""
# UPSTASH_REDIS_KV_REST_API_URL=""
# UPSTASH_REDIS_KV_REST_API_TOKEN=""
